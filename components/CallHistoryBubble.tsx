import React from 'react';

// This component is likely deprecated and its logic moved into MessageBubble in ChatScreen.
// Providing a null component to prevent import/bundling errors.
export const CallHistoryBubble: React.FC = () => {
  return null;
};

export default CallHistoryBubble;
