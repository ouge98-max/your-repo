import React from 'react';

// This component is likely deprecated and its logic moved to PaymentGatewayModal.
// Providing a null component to prevent import/bundling errors.
export const PinModal: React.FC = () => {
  return null;
};

export default PinModal;
